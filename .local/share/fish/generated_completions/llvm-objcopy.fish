# llvm-objcopy
# Autogenerated from man page /usr/share/man/man1/llvm-objcopy.1.gz
complete -c llvm-objcopy -l add-gnu-debuglink --description 'Add a . gnu_debuglink section for <debug-file> to the output.'
complete -c llvm-objcopy -l add-section --description 'Add a section named <section> with the contents of <file> to the output.'
complete -c llvm-objcopy -l binary-architecture -s B --description 'Ignored for compatibility.'
complete -c llvm-objcopy -l disable-deterministic-archives -s U --description 'Use real values for UIDs, GIDs and timestamps when updating archive member he…'
complete -c llvm-objcopy -l discard-all -s x --description 'Remove most local symbols from the output.'
complete -c llvm-objcopy -l dump-section --description 'Dump the contents of section <section> into the file <file>.'
complete -c llvm-objcopy -l enable-deterministic-archives -s D --description 'Enable deterministic mode when copying archives, i. e.'
complete -c llvm-objcopy -l help -s h --description 'Print a summary of command line options.'
complete -c llvm-objcopy -l only-keep-debug --description 'Produce a debug file as the output that only preserves contents of sections u…'
complete -c llvm-objcopy -l only-section -s j --description 'Remove all sections from the output, except for sections named <section>.'
complete -c llvm-objcopy -l redefine-sym --description 'Rename symbols called <old> to <new> in the output.'
complete -c llvm-objcopy -l redefine-syms --description 'Rename symbols in the output as described in the file <filename>.'
complete -c llvm-objcopy -l regex --description 'If specified, symbol and section names specified by other switches are treate…'
complete -c llvm-objcopy -l remove-section -s R --description 'Remove the specified section from the output.'
complete -c llvm-objcopy -l set-section-alignment --description 'Set the alignment of section <section> to <align>.'
complete -c llvm-objcopy -l strip-all-gnu --description 'Remove all symbols, debug sections and relocations from the output.'
complete -c llvm-objcopy -l strip-all -s S --description 'For ELF objects, remove from the output all symbols and non-alloc sections no…'
complete -c llvm-objcopy -l strip-debug -s g --description 'Remove all debug sections from the output.'
complete -c llvm-objcopy -l strip-symbol -s N --description 'Remove all symbols named <symbol> from the output.'
complete -c llvm-objcopy -l strip-symbols --description 'Remove all symbols whose names appear in the file <filename>, from the output.'
complete -c llvm-objcopy -l strip-unneeded-symbol --description 'Remove from the output all symbols named <symbol> that are local or undefined…'
complete -c llvm-objcopy -l strip-unneeded-symbols --description 'Remove all symbols whose names appear in the file <filename>, from the output…'
complete -c llvm-objcopy -l strip-unneeded --description 'Remove from the output all local or undefined symbols that are not required b…'
complete -c llvm-objcopy -l version -s V --description 'Display the version of the llvm-objcopy executable.'
complete -c llvm-objcopy -l wildcard -s w --description 'Allow wildcard syntax for symbol-related flags.'
complete -c llvm-objcopy -l add-symbol --description 'Add a new symbol called <name> to the output symbol table, in the section nam…'
complete -c llvm-objcopy -l allow-broken-links --description 'Allow llvm-objcopy to remove sections even if it would leave invalid section …'
complete -c llvm-objcopy -l build-id-link-dir --description 'Set the directory used by %--build-id-link-input and %--build-id-link-output.'
complete -c llvm-objcopy -l build-id-link-input --description 'Hard-link the input to <dir>/xx/xxx<suffix>, where <dir> is the directory spe…'
complete -c llvm-objcopy -l build-id-link-output --description 'Hard-link the output to <dir>/xx/xxx<suffix>, where <dir> is the directory sp…'
complete -c llvm-objcopy -l change-start -l adjust-start --description 'Add <incr> to the program\'s start address.'
complete -c llvm-objcopy -l compress-debug-sections --description 'Compress DWARF debug sections in the output, using the specified style.'
complete -c llvm-objcopy -l decompress-debug-sections --description 'Decompress any compressed DWARF debug sections in the output.'
complete -c llvm-objcopy -l discard-locals -s X --description 'Remove local symbols starting with ". L" from the output.'
complete -c llvm-objcopy -l extract-dwo --description 'Remove all sections that are not DWARF . dwo sections from the output.'
complete -c llvm-objcopy -l extract-main-partition --description 'Extract the main partition from the output.'
complete -c llvm-objcopy -l extract-partition --description 'Extract the named partition from the output.'
complete -c llvm-objcopy -l globalize-symbol --description 'Mark any defined symbols named <symbol> as global symbols in the output.'
complete -c llvm-objcopy -l globalize-symbols --description 'Read a list of names from the file <filename> and mark defined symbols with t…'
complete -c llvm-objcopy -l input-target -s I --description 'Read the input as the specified format.'
complete -c llvm-objcopy -l keep-file-symbols --description 'Keep symbols of type STT_FILE, even if they would otherwise be stripped.'
complete -c llvm-objcopy -l keep-global-symbol --description 'Make all symbols local in the output, except for symbols with the name <symbo…'
complete -c llvm-objcopy -l keep-global-symbols --description 'Make all symbols local in the output, except for symbols named in the file <f…'
complete -c llvm-objcopy -l keep-section --description 'When removing sections from the output, do not remove sections named <section…'
complete -c llvm-objcopy -l keep-symbol -s K --description 'When removing symbols from the output, do not remove symbols named <symbol>.'
complete -c llvm-objcopy -l keep-symbols --description 'When removing symbols from the output do not remove symbols named in the file…'
complete -c llvm-objcopy -l localize-hidden --description 'Make all symbols with hidden or internal visibility local in the output.'
complete -c llvm-objcopy -l localize-symbol -s L --description 'Mark any defined non-common symbol named <symbol> as a local symbol in the ou…'
complete -c llvm-objcopy -l localize-symbols --description 'Read a list of names from the file <filename> and mark defined non-common sym…'
complete -c llvm-objcopy -l new-symbol-visibility --description 'Specify the visibility of the symbols automatically created when using binary…'
complete -c llvm-objcopy -l output-target -s O --description 'Write the output as the specified format.'
complete -c llvm-objcopy -l prefix-alloc-sections --description 'Add <prefix> to the front of the names of all allocatable sections in the out…'
complete -c llvm-objcopy -l prefix-symbols --description 'Add <prefix> to the front of every symbol name in the output.'
complete -c llvm-objcopy -l preserve-dates -s p --description 'Preserve access and modification timestamps in the output.'
complete -c llvm-objcopy -l rename-section --description 'Rename sections called <old> to <new> in the output, and apply any specified …'
complete -c llvm-objcopy -l set-section-flags --description 'Set section properties in the output of section <section> based on the specif…'
complete -c llvm-objcopy -l set-start-addr --description 'Set the start address of the output to <addr>.'
complete -c llvm-objcopy -l split-dwo --description 'Equivalent to running llvm-objcopy with %--extract-dwo and <dwo-file> as the …'
complete -c llvm-objcopy -l strip-dwo --description 'Remove all DWARF . dwo sections from the output.'
complete -c llvm-objcopy -l strip-non-alloc --description 'Remove from the output all non-allocatable sections that are not within segme…'
complete -c llvm-objcopy -l strip-sections --description 'Remove from the output all section headers and all section data not within se…'
complete -c llvm-objcopy -l target -s F --description 'Equivalent to %--input-target and %--output-target for the specified format.'
complete -c llvm-objcopy -l weaken-symbol -s W --description 'Mark any global symbol named <symbol> as a weak symbol in the output.'
complete -c llvm-objcopy -l weaken-symbols --description 'Read a list of names from the file <filename> and mark global symbols with th…'
complete -c llvm-objcopy -l weaken --description 'Mark all defined global symbols as weak in the output.'

